import { SumDirective } from './sum.directive';

describe('SumDirective', () => {
  it('should create an instance', () => {
    const directive = new SumDirective();
    expect(directive).toBeTruthy();
  });
});
